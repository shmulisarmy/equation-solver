import { render } from "solid-js/web";
import EndOfLevel from "./index";

describe("EndOfLevel component", () => {
  test("it renders without crashing", () => {
    render(() => <EndOfLevel />);
  });
});
